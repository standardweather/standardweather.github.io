Upload ALL to repo root:
index.html, logo.png, README.md, .nojekyll, robots.txt, sitemap.xml
Replace existing files. Delete logo.jpg if present.
After live: test the contact form once (confirm FormSubmit email if prompted).
